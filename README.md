# Bbox_in_R
small functions in R that make programming and analysis easier


alldependency_recursive.R: extract the list of all necessary libraries (e.g. dependencies, imports) when installing an R library. It is useful and help you get a complete list of libraries when you need to install libraries offline.

